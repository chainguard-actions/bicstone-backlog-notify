name: Test pull request reopened scenario
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: pull_request
      GITHUB_REF: refs/pull/2/merge
      GITHUB_HEAD_REF: feature-TEST-789
      GITHUB_BASE_REF: main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create fake pull_request reopened event payload
        run: |
          mkdir -p /tmp/github-events
          cat > /tmp/github-events/pr-reopened.json << 'EOF'
          {
            "action": "reopened",
            "number": 2,
            "pull_request": {
              "number": 2,
              "title": "TEST-789 Reopen feature",
              "body": "This PR reopens work for TEST-789",
              "state": "open",
              "merged": false,
              "html_url": "https://github.com/test-org/test-repo/pull/2",
              "head": {
                "ref": "feature-TEST-789",
                "sha": "2222222222222222222222222222222222222222"
              },
              "base": {
                "ref": "main",
                "sha": "0000000000000000000000000000000000000000"
              },
              "user": {
                "login": "test-user"
              }
            },
            "repository": {
              "full_name": "test-org/test-repo",
              "html_url": "https://github.com/test-org/test-repo"
            },
            "sender": {
              "login": "test-user"
            }
          }
          EOF
          echo "GITHUB_EVENT_PATH=/tmp/github-events/pr-reopened.json" >> $GITHUB_ENV

      - id: action
        uses: .
        continue-on-error: true
        with:
          project_key: TEST
          api_host: example.backlog.com
          api_key: fake-api-key-for-testing
          pr_reopened_comment_template: "PR reopened: {{title}}"
          pr_ready_for_review_comment_template: "PR ready for review: {{title}}"
          pr_title_reg_template: "([A-Z]+-[0-9]+)"
        env:
          GITHUB_EVENT_NAME: pull_request
          GITHUB_REF: refs/pull/2/merge
          GITHUB_HEAD_REF: feature-TEST-789
          GITHUB_BASE_REF: main
          GITHUB_EVENT_PATH: /tmp/github-events/pr-reopened.json

      - name: Assert action ran (API error is expected with fake credentials)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "action outcome: $OUTCOME"
          # The action should either succeed or fail due to API error (not setup error)
          # Both 'success' and 'failure' are acceptable — what matters is the action ran
          if [ "$OUTCOME" = "success" ] || [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action ran with outcome=$OUTCOME"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
